Continue working on the same EasyRepair backend project. Do not break existing code.
Complete bid system (phase 3: accept flow + integration).

Implement:
POST /bids/:id/accept
Rules:

only booking owner (client) can accept
booking must be PENDING
selected bid → status = ACCEPTED
all other bids → status = REJECTED
assign worker to booking using existing booking flow
booking should stop accepting new bids
Ensure:
workers cannot bid after booking is assigned
workers cannot edit rejected/accepted bids
Optional (only if easy):
send notification to selected worker
ignore others if complex
Keep:
existing chat flow unchanged
existing booking lifecycle intact
Final checks:
no duplicate logic
reuse booking/worker services where possible
After:

show updated service logic
confirm full flow:
worker bid → client sees → client accepts → booking locked
give run commands