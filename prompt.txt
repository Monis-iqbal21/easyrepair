I want to understand the exact worker notification flow when a client posts a job.

Please investigate the backend and Flutter app and explain clearly:

Main question:
When a client posts a job, how does a worker get notified, and under what exact conditions?

Check backend first:
- booking/job creation endpoint/service
- notification service
- FCM push service
- worker matching service
- worker availability/location logic
- worker skills/category matching
- any socket/event gateway logic
- database fields used for worker eligibility

Please answer these points:

1. When client posts an urgent job:
   - Is notification sent immediately?
   - Which workers are selected?
   - Does worker need to be ONLINE?
   - Does worker need matching skill/category?
   - Does worker need currentLat/currentLng?
   - Is distance/radius checked?
   - Is verificationStatus required to be VERIFIED?
   - Is account status required to be ACTIVE?
   - Is currentlyWorking=false required?
   - Does FCM token/device token need to exist?

2. When client posts a normal/scheduled job:
   - Is notification sent immediately or only 1 hour before schedule?
   - Is there any cron/queue/job scheduler for delayed notification?
   - If no scheduler exists, explain what currently happens.

3. New Jobs tab:
   - Is New Jobs tab independent from push notifications?
   - Can a worker see a job in New Jobs even if they did not receive FCM notification?
   - What endpoint loads New Jobs?
   - What filters are applied?

4. Worker online/location:
   - How does worker location get saved?
   - What endpoint saves worker location?
   - What conditions cause PATCH /workers/location to return 403?
   - Recent logs show PATCH /workers/location returns 403 Forbidden and /workers/jobs/new also returns 403 Forbidden. Find why that happens.
   - Does this mean worker is not verified/active/eligible, or logged in as wrong role/client?

5. Worker bid map:
   - If a worker bids but is offline, do we still return their last known location?
   - Which fields are returned in booking bids response?
   - currentLat/currentLng/locationUpdatedAt should show last known worker location even offline, if saved.

6. Give me a simple flow diagram in text:
Client posts job → backend creates booking → finds eligible workers → sends notification → worker sees notification/New Jobs tab → places bid.

7. Then give me the exact files/functions involved.

8. Do not change code yet.
Only investigate and explain current behavior.
If something is missing or broken, list it under "Issues found" and "Recommended fixes".